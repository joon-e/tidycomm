# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages

matrix:
  include:
    - os: linux
      dist: trusty
      r: release
      r_github_packages:
        - r-lib/covr
      addons:
        apt:
          packages:
            - libgsl0-dev
    - os: osx


notifications:
  email:
    on_success: change
    on_failure: change

after_success:
    - Rscript -e 'covr::codecov()'
